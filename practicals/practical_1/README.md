# CHAPTER 8: DESIGN A URL SHORTENER

## Step 1 - Understand the problem and establish design scope

The chapter starts by framing URL shortener design through clarification questions and scope setting, which I found very useful because it converts an open-ended prompt into concrete constraints like 100 million new URLs per day, read-heavy traffic at about 10:1, allowed characters as base62, immutable links, and long-term storage needs of roughly 365 billion records and around 365 TB, and the author uses these numbers to define the core use cases of shortening, redirecting, and building for high availability and fault tolerance; my understanding is that strong design begins with shared assumptions and quick estimations before any architecture choice, but I am still confused about how strict these assumptions should be in real interviews when data may be uncertain, so I want to explore better estimation strategies, sensitivity analysis, and how to justify assumption changes under interviewer pressure, and in short this step proves that good system design starts with scope clarity and measurable scale.

## Step 2 -  Propose high-level design and get buy-in

The high-level design is presented through REST APIs and request flow, where POST /api/v1/data/shorten accepts a long URL and returns a short one, GET /api/v1/{shortUrl} handles redirection, and the author compares 301 versus 302 by trade-off rather than rule, showing that 301 reduces server load through browser caching while 302 keeps traffic observable for analytics, and this is connected to a simple hash-table mental model of resolving shortURL to longURL; I understand that this stage is about securing buy-in with clear interfaces and behavior choices before deep implementation, but I am still uncertain about when analytics value should outweigh caching efficiency in production systems, so I want to explore redirect strategy selection by product goals, cache-control behavior across browsers, and API versioning decisions under growth, and overall this step shows that early architecture quality depends on explicit API design and clearly defended trade-offs.

## Step 3 - Design deep dive

The deep dive improves realism by replacing in-memory hash tables with relational storage of id, shortURL, and longURL, deriving a 7-character key length from base62 capacity where 62^7 exceeds required scale, and comparing two generation strategies, hash-plus-collision-resolution versus base62 conversion, with practical notes on collision checks and bloom filters, and the final shortening flow sensibly checks existing long URLs first, generates a globally unique ID, converts it to base62, and persists the mapping, while redirect flow is optimized with cache-first reads because traffic is read-heavy; my understanding is that this section is strong because it ties math, data model, and operational flow together, but I am still confused about choosing between deterministic hashing and ID-based mapping under strict latency and consistency targets, so I want to explore distributed ID generation patterns, bloom filter false-positive impact, and database indexing strategy for hot keys, and in brief this step demonstrates that robust design comes from combining algorithmic clarity with storage and cache realities.

## Step 4 - Wrap up

The wrap-up is practical because it summarizes what was designed and then extends the discussion to real production concerns such as rate limiting against abuse, stateless web-tier scaling, database replication and sharding, analytics integration, and the constant balance among availability, consistency, and reliability, which makes the chapter feel complete beyond interview theory; I understand that a strong final answer should not stop at core flow but should include defensive controls and future scaling paths, yet I am still unsure about how to prioritize these add-ons when interview time is limited, so I want to explore concise prioritization frameworks, risk-based roadmap communication, and how to present optional improvements without losing focus, and overall this step confirms that excellent system design communication ends with thoughtful trade-offs, operational safeguards, and clear next-step evolution.